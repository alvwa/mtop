# Terminal Monitoring

A small terminal-monitoring project for collecting basic system metrics.

## Quickstart

1. Create a virtual environment and activate it:

```bash
python -m venv .venv
source .venv/bin/activate
```

2. Run the monitor:

```bash
python monitor.py
```
